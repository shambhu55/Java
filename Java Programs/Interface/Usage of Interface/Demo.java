interface Demo	// have to use interface.
{
	int a=10, b=10;		// variables are final nd static
	void m1();	// all methods are abstract and public by default.(so, 0 implement)
			// and not required to use abstract keyword
}